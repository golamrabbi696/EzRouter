{
  "name": "ezrouter-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unit tests for EzRouter",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@node-saml/node-saml": "^5.1.0",
    "sql.js": "^1.14.1",
    "undici": "^8.10.0",
    "uuid": "^13.0.0"
  }
}
